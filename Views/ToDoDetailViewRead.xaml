<UserControl x:Class="ToDoList.Views.ToDoDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             MinHeight="200">
    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 제목 (읽기 전용) -->
        <TextBlock Grid.Row="0" Text="{Binding Title}"
                   FontSize="24" FontWeight="Bold"
                   Margin="0,0,0,12"/>

        <!-- 시작/종료/메모 (읽기 전용) -->
        <StackPanel Grid.Row="1" Orientation="Vertical">
            <TextBlock Text="시작 날짜" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <TextBlock Text="{Binding StartDate, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" Margin="0,0,0,12"/>

            <TextBlock Text="종료 날짜" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <TextBlock Text="{Binding EndDate, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" Margin="0,0,0,12"/>

            <TextBlock Text="메모" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <TextBlock Text="{Binding Memo}" TextWrapping="Wrap"/>
        </StackPanel>
    </Grid>
</UserControl>